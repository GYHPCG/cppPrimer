#include <iostream>

#include "3-1head.h"
#include<cctype>
#include<string>
using namespace std;

int main()
{
    struct head k;
    // string name;
    // int a,s;
    // cin >> k.name >> k.age >> k.sex;
    // cin >> x;
    // cout << x;
    // string y = "hello";
    // string s = y + "world";
    // string str = "hello" + "hjkl";//无法编译,因为实际上，字面值“hello”并不是一个string对象，只有对象才可以
    //这样加如17行
    cout << (x < 60)?"fali":"pass";//输出1或0;跟优先级有关系,条件运算符优先级很低
    //等价于cout << (x < 60);
    //cout?"fail":"pass";
    return 0;

}
